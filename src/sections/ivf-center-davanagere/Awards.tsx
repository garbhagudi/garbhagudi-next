/* Identical to the Bangalore campaign's Awards section — no city-specific
 * content (heading is generic, all data comes from the `awards` prop). */
export { default } from 'sections/ivf-center-bangalore/Awards';
